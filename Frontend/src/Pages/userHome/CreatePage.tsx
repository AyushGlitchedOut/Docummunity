import { Box } from "@mui/material";

function CreatePage() {
  return (
    <Box sx={{ height: "100%", width: "100%", borderRadius: "20px" }}>
      CreatePage
    </Box>
  );
}

export default CreatePage;
