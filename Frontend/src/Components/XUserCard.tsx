import type { OtherUserInfo } from "../models/models";

interface XUserCardProps {
  height: string;
  width: string;
  user: OtherUserInfo;
}

export default function XUserCard(props: XUserCardProps) {
  return <></>;
}
